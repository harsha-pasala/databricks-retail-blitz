bundle:
  name: retail-app

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    retail-app-app:
      name: "retail-app"
      description: "retail-app created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
    workspace:
      profile: fe-vm
